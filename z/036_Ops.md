---
tags: [type/moc, tech/ops]
created: 2025-12-24T17:15:42
updated: 2026-01-17T22:20:28
description: "运维体系知识库入口：包含Linux、容器化、网络监控及故障排查。"
---

# 🛠️ 运维指挥中心 (Ops Dashboard)

> [!abstract] 状态概览
> `$= dv.pages("#tech/ops").length` 篇运维笔记正在维护中。
> 最近更新：`$= dv.pages("#tech/ops").sort(p => p.file.mtime, "desc").limit(1).file.link`

---

## 关键领域

```dataview
TABLE without id 
	file.link as "关键领域", 
	description as "📝 简介"
FROM #tech/ops AND #type/moc 
SORT file.name ASC
```

## 🚀 常用工具箱 (Toolbox)

*自动汇集所有打上 `#type/resource` 的运维工具，展示简介与分类。*

```dataview
TABLE without id 
	file.link as "🛠️ 工具名称", 
	description as "📝 简介", 
	resource_category as "🎯 核心能力",
	join(map(filter(file.tags, (t) => startswith(t, "#tech/ops/")), (t) => split(t, "/")[2]), ", ") as "📚 技术栈"
FROM #tech/ops
WHERE contains(resource_category, "performance") OR contains(resource_category, "monitoring")
SORT file.name ASC
```

## 领域分层索引

### Windows

```dataview
LIST rows.file.link
FROM #tech/ops AND #type/moc 
WHERE os = "windows"
GROUP BY split(file.folder, "/")[length(split(file.folder, "/")) - 1] as Folder
```

### 🐧 Linux & 系统管理

```dataview
LIST rows.file.link
FROM #tech/ops AND #type/moc 
WHERE os = "linux"
GROUP BY split(file.folder, "/")[length(split(file.folder, "/")) - 1] as Folder
```

### 🐳 容器与编排 (Docker/K8s)

```dataview
TABLE created as "创建时间", status as "状态"
FROM #tech/ops/docker OR #tech/ops/k8s
SORT file.ctime DESC
LIMIT 10
```

### 🌐 网络与监控 (Network & Monitoring)

```dataview
LIST
FROM #tech/ops/network OR #tech/ops/monitoring AND -#type/resource
SORT file.name ASC
```

### 🚑 故障排查实录 (Troubleshooting Log)

```dataview
TABLE without id
	file.link as "问题现象",
	error_code as "报错代码",
	file.ctime as "记录时间"
FROM #tech/ops AND #type/troubleshooting
SORT file.ctime DESC
```

### 📝 最近更新 (Recently Modified)

```dataview
LIST
FROM #tech/ops
SORT file.mtime DESC
LIMIT 10
```
