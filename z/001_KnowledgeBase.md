---
tags:
  - type/moc
  - status/evergreen
description: 知识库管理体系概念相关内容
created: 2025-12-08T00:00:00
updated: 2025-12-08T00:00:00
category: KnowledgeBase
---

> [!info] **上级索引**
> [[000_Home]]

---

# 🗂️ 知识库管理体系 MOC

> 本页面整合了知识库管理的所有核心文档，帮助你高效管理和使用知识库。

---



## 扁平化+tags

| 文档                     | 说明                     |
| ------------------------ | ------------------------ |
| [[知识库标签体系设计]]   | 掌握四维度标签体系       |
| [[知识库扁平化管理指南]] | 完整的操作手册           |
| [[知识库链接功能]]       | 学习双向链接的高级用法   |

- [[知识管理-个人看法]] - 个人知识管理的基础理念

- [[obsidian笔记链接]] - 知识图谱组织方法
  - Hub-and-Spoke 模型
  - MOC 设计实践

## tags 管理

![[知识库标签体系设计#🏛️ 核心逻辑：四大维度 (The 4-Dimension System)]]

### Domain（领域维度）

#### Tech 相关

```dataviewjs
// ============================================================
// 1. 定义你要拆分的板块 (在这里增删改查)
// prefix: 标签前缀, title: 表格标题
// ============================================================
const sections = [
    { prefix: "#tech/lang",     title: "⌨️ 编程语言 (Languages)" },
    { prefix: "#tech/ops",      title: "⚙️ 运维与部署 (Ops)" },
    { prefix: "#tech/ai",       title: "🤖 人工智能 (AI)" },
    { prefix: "#tech/dev",      title: "🏗️ 开发框架 (Dev/Framework)" },
    { prefix: "#tech/database", title: "💾 数据库 (DB)" },
    { prefix: "#tech/tool",     title: "🛠️ 工具 (Tools)" }
];

// ============================================================
// 2. 定义标签的详细说明 (字典)
// ============================================================
const tagDescriptions = {
    "#tech/lang/python":    "🐍 Python",
    "#tech/lang/javascript":"🟨 JavaScript / TS",
    "#tech/lang/go":        "🐹 Go 语言",
    "#tech/lang/rust":      "🦀 Rust",
    "#tech/ops/docker":     "🐳 Docker 容器",
    "#tech/ops/k8s":        "☸️ Kubernetes",
    "#tech/ops/linux":      "🐧 Linux",
    "#tech/ai/llm":         "🧠 大语言模型",
    "#tech/ai/prompt":      "💬 提示词工程"
    // 你可以在这里继续补充...
};

// ============================================================
// 3. 循环渲染逻辑 (自动生成多个表格)
// ============================================================

for (let section of sections) {
    // 3.1 获取属于当前板块的所有标签
    let tags = dv.pages(section.prefix)
        .flatMap(p => p.file.tags)
        .where(t => t.startsWith(section.prefix))
        .distinct()
        .sort();

    // 如果这个板块下没有标签，直接跳过，不渲染空表格
    if (tags.length === 0) continue;

    // 3.2 构建行数据
    let rows = tags.map(t => {
        let count = dv.pages(t).length;
        // 获取说明，如果字典里没写，就自动截取最后一段
        let desc = tagDescriptions[t] || t.split("/").pop(); 
        return [t, desc, count];
    });

    // 3.3 按数量倒序排列
    rows.sort((a, b) => b[2] - a[2]);

    // 3.4 渲染标题和表格
    dv.header(3, section.title); // 生成三级标题
    dv.table(["Tag (标签)", "说明", "笔记数"], rows);
}
```

### Type（类型维度）


```dataviewjs
// --- 1. 在这里定义你的标签说明 ---
const tagDescriptions = {
    "#type/concept": "🧠 概念 / 理论原理 (What)",
    "#type/howto": "🔧 实操 / 教程指南 (How)",
    "#type/resource": "📦 资源 / 工具素材",
    "#type/moc": "🗺️ 索引 / 知识地图",
    "#type/snippet": "💻 代码片段",
    "#type/journal": "📅 日记 / 时间记录",
    "#type/troubleshoot": "🐛 排错 / 问题修复"
};

// --- 2. 获取标签并计算数据 ---
// 获取所有 #type 开头的标签
let tags = dv.pages("#type")
    .flatMap(p => p.file.tags)
    .where(t => t.startsWith("#type"))
    .distinct()
    .sort();

// 构建表格行数据
let rows = tags.map(t => {
    // 计算该标签下的笔记数量
    let count = dv.pages(t).length;
    // 从上面的字典里取说明，如果没有定义，显示 "-"
    let desc = tagDescriptions[t] || "-"; 
    
    return [t, desc, count];
});

// --- 3. 渲染表格 ---
dv.table(
    ["Tag (标签)", "含义说明", "笔记数量"], 
    rows
);
```

### Status（状态维度）

```dataviewjs
// --- 1. 定义状态标签的含义与 Emoji ---
// 这里对应你截图右侧的 status 层级
const statusMap = {
    "#status/seed":      "🌱 种子 / 灵感雏形 (Seed)",
    "#status/growing":   "🌿 生长 / 正在加工 (Growing)",
    "#status/evergreen": "🌲 常青 / 成熟定稿 (Evergreen)",
    "#status/archived":  "🗃️ 归档 / 不再维护" // 预留一个归档状态
};

// --- 2. 获取数据 ---
// 只获取 #status 下的页面和标签
let tags = dv.pages("#status")
    .flatMap(p => p.file.tags)
    .where(t => t.startsWith("#status")) // 过滤掉 #tech 等其他标签
    .distinct() // 去重
    .sort();    // 排序

// --- 3. 构建表格行 ---
let rows = tags.map(t => {
    // 统计数量
    let count = dv.pages(t).length;
    
    // 获取说明，如果字典里没定义，就显示默认名称
    let desc = statusMap[t] || "未定义状态";
    
    // 返回一行数据: [标签, 说明, 数量]
    return [t, desc, count];
});

// --- 4. 渲染表格 ---
dv.header(3, "Status 状态分布"); // 可选：加个小标题
dv.table(
    ["Tag (状态)", "含义说明", "笔记数量"], 
    rows
);
```
